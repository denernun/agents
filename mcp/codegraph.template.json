{
  "mcpServers": {
    "codegraph": {
      "command": "cmd",
      "args": ["/c", "codegraph", "serve", "--mcp", "--path", "{{REPO}}"],
      "cwd": "{{REPO}}",
      "type": "stdio",
      "env": { "DO_NOT_TRACK": "1" }
    }
  }
}
