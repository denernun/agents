{
  "mcpServers": {
    "mongodb": {
      "command": "{{NODE}}",
      "args": ["{{MDB_MCP_ENTRY}}"],
      "env": {
        "MDB_MCP_CONNECTION_STRING": "{{MDB_MCP_CONNECTION_STRING}}",
        "MDB_MCP_READ_ONLY": "true"
      },
      "type": "stdio"
    }
  }
}
